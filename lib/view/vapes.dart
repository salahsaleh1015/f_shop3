import 'package:flutter/material.dart';
class Vapes extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Vapes",style: TextStyle(fontSize: 50),),),
    );
  }
}
